// Prisma client stub — not used in JSON-based API mode
// In production with PostgreSQL, uncomment the real import:
// import { PrismaClient } from '@prisma/client';
// const prisma = new PrismaClient();
// export default prisma;

const prisma = {} as any;
export default prisma;
